# Grab the registry password from ENV (GitHub Personal Access Token for ghcr.io)
KAMAL_REGISTRY_PASSWORD=$GITHUB_PERSONAL_ACCESS_TOKEN

# Rails master key for decrypting credentials
RAILS_MASTER_KEY=$(cat config/master.key)
